# Documentation build requirements.
#
# Only the docs toolchain lives here. Everything autodoc needs to import the package comes
# from the package itself, which .readthedocs.yaml installs from this repository, so
# pyproject.toml stays the single source for runtime dependencies.
sphinx>=7.0
pydata-sphinx-theme>=0.15
sphinx-design>=0.5
sphinx-copybutton>=0.5
myst-parser>=2.0
nbsphinx>=0.9
ipykernel>=6.0
